import{_ as n,c as s,a,o}from"./app-CJ6ihum5.js";const c="/assets/image-20260802222608006-CDjZ3RGQ.png",t={};function i(l,e){return o(),s("div",null,[...e[0]||(e[0]=[a(`<h2 id="核心流程分析" tabindex="-1"><a class="header-anchor" href="#核心流程分析"><span>核心流程分析</span></a></h2><h3 id="将源码转化为通用ast" tabindex="-1"><a class="header-anchor" href="#将源码转化为通用ast"><span>将源码转化为通用AST</span></a></h3><blockquote><p>目标：将源码转化为统一表示的通用AST</p></blockquote><h4 id="_1-源码-cst" tabindex="-1"><a class="header-anchor" href="#_1-源码-cst"><span>1. 源码 -&gt; CST</span></a></h4><p>在这一步中，会先经过tree-sitter或Semgrep自己的Pfff作为解析器，将源码转化为CST。</p><blockquote><p>注意：CST 由 <code>ocaml-tree-sitter</code> 根据各语言的 grammar 自动生成 OCaml 类型。因此，使用<code>semgrep show dump-cst</code>得到的结果，会与直接使用tree-sitter项目得到的结果不同。差异点主要来自日志打印的格式和semgrep为了自己特殊语法而实现的grammar</p></blockquote><p>使用semgrep提供的命令<code>semgrep show dump-cst &lt;lang&gt; &lt;file&gt;</code>可以查看CST信息。例如，对于Python的<code>print(&quot;hello&quot;)</code>得到的结果如下。可以看到，CST相比AST的特点就是保留了<code>(</code>、<code>)</code>、<code>&quot;</code>等源码特征</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text" data-title="text"><pre><code><span class="line">[</span>
<span class="line">  Simple_stmts (</span>
<span class="line">    (</span>
<span class="line">      Exp_stmt (</span>
<span class="line">        Exp (</span>
<span class="line">          Prim_exp (</span>
<span class="line">            Call (</span>
<span class="line">              (</span>
<span class="line">                Choice_choice_print (</span>
<span class="line">                  Choice_print (</span>
<span class="line">                    Print &quot;print&quot;</span>
<span class="line">                  )</span>
<span class="line">                )</span>
<span class="line">                Arg_list (</span>
<span class="line">                  (</span>
<span class="line">                    &quot;(&quot;</span>
<span class="line">                    Some (</span>
<span class="line">                      Exp (</span>
<span class="line">                        Prim_exp (</span>
<span class="line">                          Str (</span>
<span class="line">                            (</span>
<span class="line">                              &quot;\\&quot;&quot;</span>
<span class="line">                              [</span>
<span class="line">                                Str_content (</span>
<span class="line">                                  [</span>
<span class="line">                                    Str_content_ &quot;hello&quot;</span>
<span class="line">                                  ]</span>
<span class="line">                                )</span>
<span class="line">                              ]</span>
<span class="line">                              &quot;\\&quot;&quot;</span>
<span class="line">                            )</span>
<span class="line">                          )</span>
<span class="line">                        )</span>
<span class="line">                      )</span>
<span class="line">                      []</span>
<span class="line">                    )</span>
<span class="line">                    None</span>
<span class="line">                    &quot;)&quot;</span>
<span class="line">                  )</span>
<span class="line">                )</span>
<span class="line">              )</span>
<span class="line">            )</span>
<span class="line">          )</span>
<span class="line">        )</span>
<span class="line">      )</span>
<span class="line">      []</span>
<span class="line">      None</span>
<span class="line">      &quot;&quot;</span>
<span class="line">    )</span>
<span class="line">  )</span>
<span class="line">]</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h4 id="_2-cst-语言特定ast-可选层" tabindex="-1"><a class="header-anchor" href="#_2-cst-语言特定ast-可选层"><span>2. CST -&gt; 语言特定AST（可选层）</span></a></h4><p>这一步的目的是去除语法糖。以Java为例，以下方法是将几个特殊类型转化为<code>TBasic</code>统一表示</p><div class="language-ocaml line-numbers-mode" data-highlighter="prismjs" data-ext="ocaml" data-title="ocaml"><pre><code><span class="line"><span class="token keyword">let</span> integral_type <span class="token punctuation">(</span>env <span class="token punctuation">:</span> env<span class="token punctuation">)</span> <span class="token punctuation">(</span>x <span class="token punctuation">:</span> CST<span class="token punctuation">.</span>integral_type<span class="token punctuation">)</span> <span class="token operator">=</span></span>
<span class="line">  <span class="token keyword">match</span> x <span class="token keyword">with</span></span>
<span class="line">  <span class="token operator">|</span> <span class="token variant symbol">\`Byte</span> tok <span class="token operator">-&gt;</span> TBasic <span class="token punctuation">(</span>str env tok<span class="token punctuation">)</span> <span class="token comment">(* &quot;byte&quot; *)</span></span>
<span class="line">  <span class="token operator">|</span> <span class="token variant symbol">\`Short</span> tok <span class="token operator">-&gt;</span> TBasic <span class="token punctuation">(</span>str env tok<span class="token punctuation">)</span> <span class="token comment">(* &quot;short&quot; *)</span></span>
<span class="line">  <span class="token operator">|</span> <span class="token variant symbol">\`Int</span> tok <span class="token operator">-&gt;</span> TBasic <span class="token punctuation">(</span>str env tok<span class="token punctuation">)</span> <span class="token comment">(* &quot;int&quot; *)</span></span>
<span class="line">  <span class="token operator">|</span> <span class="token variant symbol">\`Long</span> tok <span class="token operator">-&gt;</span> TBasic <span class="token punctuation">(</span>str env tok<span class="token punctuation">)</span> <span class="token comment">(* &quot;long&quot; *)</span></span>
<span class="line">  <span class="token operator">|</span> <span class="token variant symbol">\`Char</span> tok <span class="token operator">-&gt;</span> TBasic <span class="token punctuation">(</span>str env tok<span class="token punctuation">)</span> <span class="token comment">(* &quot;char&quot; *)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h4 id="_3-cst-语言特定ast-通用ast-ast-generic" tabindex="-1"><a class="header-anchor" href="#_3-cst-语言特定ast-通用ast-ast-generic"><span>3. CST/语言特定AST -&gt; 通用AST(AST_generic)</span></a></h4><p>Semgrep支持几十种语言，因此最终需要统一转化为通用AST，再进行匹配。还是以<code>print(&quot;hello&quot;)</code>为例，使用<code>semgrep show dump-ast &lt;lang&gt; &lt;file&gt;</code>就能得到通用的AST结果，如下所示。其中，Id信息有很多字段还是空的（如<code>id_resolved</code>、<code>id_type</code>），需要等到命名解析后才填充完成。</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text" data-title="text"><pre><code><span class="line">Pr(</span>
<span class="line">  [ExprStmt(</span>
<span class="line">     Call(</span>
<span class="line">       N(</span>
<span class="line">         Id((&quot;print&quot;, ()),</span>
<span class="line">           {id_info_id=1; id_flags=Ref(0); id_resolved_alternative=Ref(</span>
<span class="line">            []); id_resolved=Ref(None); id_type=Ref(None);</span>
<span class="line">            id_svalue=Ref(Unknown); })), [Arg(L(String((&quot;hello&quot;, ()))))]), ())])</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h4 id="_4-ast-generic-命名解析后的-ast-generic" tabindex="-1"><a class="header-anchor" href="#_4-ast-generic-命名解析后的-ast-generic"><span>4. AST_generic -&gt; 命名解析后的 AST_generic</span></a></h4><p>这一步主要是做命名空间解析，使得同名变量在不同作用域中能被Semgrep识别出来。产物仍然是 <code>AST_generic</code>，只是节点内部的信息字段被填充了。</p><h4 id="_5-il-仅数据流-污点分析需要" tabindex="-1"><a class="header-anchor" href="#_5-il-仅数据流-污点分析需要"><span>5. IL（仅数据流/污点分析需要）</span></a></h4><p>定义了一套更接近三地址码的类型，把通用AST拆分为 <code>exp</code>（无副作用）、<code>instr</code>（有副作用的指令）、<code>stmt</code>（控制流）三层</p><h3 id="规则文件的解析与预处理" tabindex="-1"><a class="header-anchor" href="#规则文件的解析与预处理"><span>规则文件的解析与预处理</span></a></h3><blockquote><p>目标：将YAML/JSON/Jsonnet格式的规则文件，转化为匹配引擎可以理解和执行的内部结构</p></blockquote><img src="`+c+`" alt="image-20260802222608006" style="zoom:50%;"><h4 id="_1-规则文件-通用ast-ast-generic" tabindex="-1"><a class="header-anchor" href="#_1-规则文件-通用ast-ast-generic"><span>1. 规则文件 -&gt; 通用AST(AST_generic)</span></a></h4><p>提取不同来源规则文件（yaml、json、jsonnet）的结构化数据，并统一都转化成通用AST。其实规则文件的各项配置本来与源码是关系不大的，只有<code>patterns/pattern-sources/...</code>才是真正有直接关系的，但是这里为了让三种<strong>不同来源的规则文件在后续步骤能复用同一套代码</strong>，因此选择先直接转化为通用AST。</p><h4 id="_2-从通用ast里-取出-规则字段-字典驱动的解析" tabindex="-1"><a class="header-anchor" href="#_2-从通用ast里-取出-规则字段-字典驱动的解析"><span>2. 从通用AST里&quot;取出&quot;规则字段（字典驱动的解析）</span></a></h4><p>基于通用AST解析<code>rules</code>数组（如下代码块所示，一个规则文件可能有多个规则），然后再遍历解析每一个rule，提取每一个合法key并进行value合法性校验，最后检查残留的字段（拼写错误/字段非法等）。最终，每一个规则都会拼接成一个Rule.t对象。</p><p>在这一步中，<strong>解析<code>languages</code>能得出<code>target_selector</code>/<code>target_analyzer</code>（决定后面 <code>pattern</code> 用哪个语言的解析器）</strong>，然后调用 <code>parse_mode</code> 按 <code>mode</code>（或者是否存在 <code>taint</code> key）分流到不同的字段解析逻辑</p><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml" data-title="yml"><pre><code><span class="line"><span class="token key atrule">rules</span><span class="token punctuation">:</span></span>
<span class="line">  <span class="token punctuation">-</span> <span class="token key atrule">id</span><span class="token punctuation">:</span> eval</span>
<span class="line">    <span class="token key atrule">message</span><span class="token punctuation">:</span> Detect function eval</span>
<span class="line">    <span class="token key atrule">severity</span><span class="token punctuation">:</span> HIGH</span>
<span class="line">    <span class="token key atrule">languages</span><span class="token punctuation">:</span></span>
<span class="line">      <span class="token punctuation">-</span> python</span>
<span class="line">    <span class="token key atrule">pattern</span><span class="token punctuation">:</span> eval(<span class="token punctuation">...</span>)</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h4 id="_3-把-pattern-patterns-pattern-either-递归组装成-rule-formula-布尔公式树" tabindex="-1"><a class="header-anchor" href="#_3-把-pattern-patterns-pattern-either-递归组装成-rule-formula-布尔公式树"><span>3. 把 <code>pattern</code>/<code>patterns</code>/<code>pattern-either</code>/... 递归组装成 <code>Rule.formula</code>（布尔公式树）</span></a></h4><p>无论是 search 模式还是 taint 模式内的每个 source/sink/sanitizer/propagator，最终都要解析出一个 <code>Rule.formula</code>（定义在 <code>Rule.ml</code>，含 <code>P</code>/<code>And</code>/<code>Or</code>/<code>Not</code>/<code>Inside</code>/<code>Anywhere</code> 几种 <code>formula_kind</code>）。几个核心关键语法情况如下</p><ul><li><code>&quot;pattern&quot;</code> → 直接把字符串值送去 <code>get_pattern</code>（即<strong>真正解析成目标语言AST</strong>，见下一节），包成叶子节点 <code>R.P pat</code>；</li><li><code>&quot;pattern-not&quot;</code> → 递归解析子值为 <code>formula</code>，包成 <code>R.Not (tok, formula)</code>；</li><li><code>&quot;pattern-inside&quot;</code> → 包成 <code>R.Inside (tok, formula)</code>；</li><li><code>&quot;pattern-not-inside&quot;</code> → <code>Not (Inside (...))</code>；</li><li><code>&quot;pattern-either&quot;</code> → 对列表里每一项递归解析出子 <code>formula</code>，拼成 <code>R.Or (tok, formulae)</code>（同时会检查 <code>pattern-either</code> 下不能直接放 <code>pattern-not</code>，否则报 <code>InvalidNotInOr</code>）；</li><li><code>&quot;patterns&quot;</code> → 对列表里每一项做更细的分类：不带任何 <code>metavariable-*</code>/<code>focus-metavariable</code> key 的子项当普通嵌套 <code>formula</code>，使用<code>And</code>连接；<code>focus-metavariable</code> 单独归到 focus 列表；<code>metavariable-*</code> 这些&quot;过滤条件&quot;归到 <code>conditions</code> 列表；</li><li><code>&quot;pattern-regex&quot;</code>/<code>&quot;pattern-not-regex&quot;</code> → 不解析成 AST，而是编译成正则表达式；</li><li><code>&quot;semgrep-internal-pattern-anywhere&quot;</code> → 包成 <code>R.Anywhere</code>。</li></ul><p>可以看到，<strong>规则解析在这一层做的事情就是把 YAML 的嵌套 key（<code>patterns</code>/<code>pattern-either</code>/<code>pattern-not</code>等）翻译成一棵布尔表达式树 <code>Rule.formula</code></strong>。<code>metavariable-regex</code> 等<code>metavariable-*</code>的约束被拎出来单独放进 <code>conditions</code> 字段（不参与布尔结构，只在匹配阶段做过滤），<code>focus-metavariable</code> 同理被拎到 <code>focus</code> 字段。</p><p>而 taint 模式则是对 <code>sources</code>/<code>sinks</code>/<code>sanitizers</code>/<code>propagators</code> 里的每一项分别走上面同一套逻辑，得到各自的 <code>source_formula</code>/<code>sink_formula</code>/... （本质上也是 <code>Rule.formula</code>），再加上 taint 专属字段（<code>requires</code>、<code>by-side-effect</code>、<code>label</code>、<code>exact</code>、<code>at-exit</code>等）</p><p>也就是说，<strong>search 模式只有一棵 formula 树，taint 模式有 N 棵 formula 树（每个 source/sink/sanitizer/propagator 各一棵），规则解析器对每棵树用的是同一套递归解析代码</strong>，区别只是在外层多了一层 taint 专属的字段。</p><h4 id="_4-叶子节点里的-pattern-字符串才真正进入-源码解析-链路" tabindex="-1"><a class="header-anchor" href="#_4-叶子节点里的-pattern-字符串才真正进入-源码解析-链路"><span>4. 叶子节点里的 <code>pattern</code> 字符串才真正进入&quot;源码解析&quot;链路</span></a></h4><p>上面第三步递归到叶子 <code>&quot;pattern&quot;</code> key 时，取出的是一个字符串（如 <code>&quot;eval(...)&quot;</code>），这时候才按规则的 <code>target_analyzer</code>（在解析 <code>languages:</code> 字段时就确定了）分流：</p><ul><li>如果目标是某个编程语言，使用的是**&quot;源码解析&quot;链路**（见上一节&quot;将源码转化为通用AST&quot;），对每种语言分派到该语言真正的解析器（tree-sitter/pfff），并最终转化为 <code>AST_generic</code>。但注意这里<strong>没有命名解析和 IL 转换</strong>，因为 pattern 只是一段孤立的代码片段（甚至可能语法不完整），常规目标文件解析要做的&quot;完整程序&quot;步骤（如构建符号表、生成 CFG/IL）在这里不适用，只做到 <code>AST_generic</code> 这一层，并且要处理 <code>...</code>（Ellipsis）、<code>$X</code>（Metavar）这些 semgrep 特有语法；</li><li>如果是 <code>LRegex</code>，走 <code>parse_regexp</code> 编译成正则；</li><li>如果是 <code>LSpacegrep</code>/<code>LAliengrep</code>，走各自的通用文本模式编译器。</li></ul><h3 id="pattern-search-模式的匹配流程" tabindex="-1"><a class="header-anchor" href="#pattern-search-模式的匹配流程"><span>Pattern（search）模式的匹配流程</span></a></h3><h4 id="_1-叶子模式匹配" tabindex="-1"><a class="header-anchor" href="#_1-叶子模式匹配"><span>1. 叶子模式匹配</span></a></h4><p>先扫描规则公式的所有叶子节点，然后启用一个visitor模式，<strong>遍历每一个叶子节点，尝试匹配每一个规则范围内的代码文件的<code>AST_generic</code></strong>，每次匹配成功产生一个结果对象。</p><h4 id="_2-公式求值" tabindex="-1"><a class="header-anchor" href="#_2-公式求值"><span>2. 公式求值</span></a></h4><p>递归地按 <code>formula_kind</code> 结构组合这些 range：</p><ul><li><code>P</code> → 直接取该 pattern 的匹配 range 列表；</li><li><code>Or</code> → 各子公式 range 列表拼接；</li><li><code>And</code> → 分离出 positive 项（<code>pattern</code>）、<code>pattern-inside</code> 项、negative 项（<code>pattern-not</code>），先对 positive 项做 range 交集（按包含关系判断并合并 metavariable 绑定），再减去 negative 项的 range；</li><li><code>Inside</code>/<code>Anywhere</code> → 先递归求子公式得到range，然后标记 <code>kind</code>（用于后续 <code>And</code> 时区分&quot;inside 类型&quot;参与交集的语义）</li></ul><p>之后 <code>evaluate_formula</code> 再应用 <code>conditions</code>（<code>metavariable-regex</code>/<code>metavariable-comparison</code>/<code>metavariable-pattern</code> 等过滤条件）、<code>focus-metavariable</code>（把 range 收窄到某个元变量的范围）、<code>fix</code>/<code>as</code> 绑定</p><h4 id="_3-元变量绑定的传播-一致性检查" tabindex="-1"><a class="header-anchor" href="#_3-元变量绑定的传播-一致性检查"><span>3. 元变量绑定的传播/一致性检查</span></a></h4><p>在上一步的positive项中完成，交集时要求同名元变量在两个 range 里绑定&quot;语义相同&quot;的代码</p><p>最终剩下的 range 就是这条规则在该文件里的&quot;发现&quot;（finding）</p><h3 id="taint模式的匹配流程" tabindex="-1"><a class="header-anchor" href="#taint模式的匹配流程"><span>Taint模式的匹配流程</span></a></h3><p>Taint 模式<strong>同样复用 search 模式的公式求值机制来匹配 source/sink/propagator/sanitizer 各自的 pattern</strong>，但匹配之后走的是数据流分析，而不是纯 range 集合运算。</p><h4 id="_1-规则规格匹配" tabindex="-1"><a class="header-anchor" href="#_1-规则规格匹配"><span>1. 规则规格匹配</span></a></h4><p>对 <code>sources</code>/<code>sinks</code>/<code>sanitizers</code>/<code>propagators</code> 里每一条的 <code>formula</code>，调用与 search 模式相同的规则，得到各自在文件中匹配到的 range 集合</p><h4 id="_2-il和cfg构建" tabindex="-1"><a class="header-anchor" href="#_2-il和cfg构建"><span>2. IL和CFG构建</span></a></h4><p>对每个函数定义、类初始化代码、顶层语句分别转成 IL并建 CFG</p><h4 id="_3-数据流转移匹配" tabindex="-1"><a class="header-anchor" href="#_3-数据流转移匹配"><span>3. 数据流转移匹配</span></a></h4><p>对 CFG 每个节点（instr/cond/return等）</p><ul><li>用 <code>orig_is_best_source</code>/<code>lval_is_source</code> 检查该表达式/左值是否命中某个 source spec 的匹配 range，命中则把对应 <code>label</code> 的 taint 加入当前污点集合（<code>taints_of_matches</code>），并区分 <code>by-side-effect</code> 的处理方式（源变量本身被污染 vs. 表达式返回污染值）</li><li>用 <code>orig_is_sanitizer</code> 检查是否命中 sanitizer spec，命中则清除污点（<code>Sanitized</code>），并处理 <code>by-side-effect</code>/<code>not_conflicting</code> 语义</li><li>用 <code>handle_taint_propagators</code> 检查是否命中 propagator 的 <code>from</code>/<code>to</code> range，命中则把 <code>from</code> 处已有的污点传播到 <code>to</code> 处（跨变量、跨调用传播污点，不局限于表达式子树）</li><li>用 <code>lval_is_sink</code>/<code>orig_is_best_sink</code> 检查是否命中 sink spec，命中且当前污点集合非空（且满足 <code>requires</code> 前置条件表达式），则记录一个 &quot;发现&quot;，并保留完整的 taint call trace（source→...→sink 的调用链）</li></ul><blockquote><p>具体细节会更复杂，这里做了简化</p></blockquote><h2 id="常见问题" tabindex="-1"><a class="header-anchor" href="#常见问题"><span>常见问题</span></a></h2><h3 id="规则的解析和源码的解析有什么区别" tabindex="-1"><a class="header-anchor" href="#规则的解析和源码的解析有什么区别"><span>规则的解析和源码的解析有什么区别</span></a></h3><ul><li>源码解析的输入是完整的目标程序，输出要支持后续命名解析和 IL/CFG 构建，服务于数据流分析</li><li>规则解析的输入是 YAML/JSON 这种&quot;配置数据&quot;，绝大部分字段解析只是字典取值和结构校验，只有 <code>pattern:</code> 相关字符串会&quot;借用&quot;源码解析器的前半段（CST→语言AST→AST_generic），但止步于此，产出的是可被结构化匹配（<code>Pattern_vs_code.ml</code>）直接使用的 pattern AST，不需要也不会做命名消解或 IL 化</li></ul>`,59)])])}const p=n(t,[["render",i]]),r=JSON.parse('{"path":"/series/sdl/SAST/Semgrep/semgrep03-hexinyuanli.html","title":"03 Semgrep核心原理简析","lang":"zh-CN","frontmatter":{"title":"03 Semgrep核心原理简析","date":"2026/08/03"},"headers":[{"level":2,"title":"核心流程分析","slug":"核心流程分析","link":"#核心流程分析","children":[{"level":3,"title":"将源码转化为通用AST","slug":"将源码转化为通用ast","link":"#将源码转化为通用ast","children":[]},{"level":3,"title":"规则文件的解析与预处理","slug":"规则文件的解析与预处理","link":"#规则文件的解析与预处理","children":[]},{"level":3,"title":"Pattern（search）模式的匹配流程","slug":"pattern-search-模式的匹配流程","link":"#pattern-search-模式的匹配流程","children":[]},{"level":3,"title":"Taint模式的匹配流程","slug":"taint模式的匹配流程","link":"#taint模式的匹配流程","children":[]}]},{"level":2,"title":"常见问题","slug":"常见问题","link":"#常见问题","children":[{"level":3,"title":"规则的解析和源码的解析有什么区别","slug":"规则的解析和源码的解析有什么区别","link":"#规则的解析和源码的解析有什么区别","children":[]}]}],"git":{"createdTime":1785768488000,"updatedTime":1785768656000,"contributors":[{"name":"bertramoon","email":"bertramoon@126.com","commits":1}]},"filePathRelative":"series/sdl/SAST/Semgrep/semgrep03-核心原理.md"}');export{p as comp,r as data};
